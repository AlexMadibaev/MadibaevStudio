# Madibaev Studio

The active site is the Next.js application in `mgs-next`.

For VPS deployment use only the clean Docker package in [`vps`](./vps). It contains the same current application plus the production Docker/HTTPS/persistent-storage configuration. The old Express/MySQL/Redis prototype has been removed from the repository root.

- Application source: `mgs-next/`
- VPS deploy package: `vps/`
